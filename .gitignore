# Бинарные файлы моделей — хостятся в GitHub Releases
models/**/*.bin
models/**/*.wasm
# Оставляем vocab файлы в репо (маленькие, ~0.86MB, нужны для инициализации)
# models/**/*.spm  ← не игнорируем

# Бэкапы
content_script_backup*.js

# Agent context — local only
CLAUDE.md

# OS
.DS_Store
Thumbs.db
